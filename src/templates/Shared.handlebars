module {{RootModule}}.Shared

open System
open System.Collections.Generic
open Elmish
open Feliz

type Model = unit

type Msg = | NoOp

let init () = (), Cmd.none

let update msg model =
    match msg with
    | NoOp -> model, Cmd.none
