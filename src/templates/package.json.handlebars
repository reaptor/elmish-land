{
  "name": "{{ProjectName}}",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "dotnet tool restore; dotnet restore",
    "vite": "vite",
    "elmish-land:start": "dotnet fable --run vite",
    "elmish-land:build": "dotnet fable --noCache --run vite build"
  }
}
