{
  "name": "{{PROJECT_NAME}}",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "dotnet tool restore",
    "prestart": "npm install",
    "start": "dotnet fable watch --run vite",
    "build": "dotnet fable --run vite"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^5.0.2"
  }
}
